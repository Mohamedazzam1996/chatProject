<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mazzam.chatproject">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name=".MyApplication"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".chat.splash"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".chat.chatRoomActivity"
            android:label="@string/title_activity_chat_room"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".chat.Registeration"
            android:label="@string/Registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".chat.HomeActivity"
            android:theme="@style/NoActionBar"
            android:label="Rooms Available"
            />
        <activity
            android:name=".chat.AddingRoomActivity"
            android:label="Adding Room" />
        <activity android:name=".chat.login"></activity>
    </application>

</manifest>